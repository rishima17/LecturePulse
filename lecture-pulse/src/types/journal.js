/**
 * @typedef {Object} JournalNote
 * @property {string} id - Unique identifier (UUID)
 * @property {string} sessionCode - The lecture session code
 * @property {string} [title] - Optional title of the note
 * @property {string} content - Text content of the note
 * @property {string} createdAt - ISO timestamp when the note was created
 * @property {string} updatedAt - ISO timestamp when the note was last updated
 * @property {boolean} starred - Mark note as important
 * @property {boolean} reviseLater - Mark note as revise later
 */

export const Types = {};
